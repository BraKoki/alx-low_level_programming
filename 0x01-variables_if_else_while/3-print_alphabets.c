#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 * main -Prints the alphabet in lowercase, and then in uppercase
 *
 * Return: Always 0 (GoodGame)
 */

int main(void)
{
	int ch;

	for (ch = 'a'; ch <= 'z'; ch++)
		for (ch = 'A'; 'Z'; ch++)
			putchar(ch)
putchar('\n')
return (0);
}
